import Contacts from "../Contacts/Contacts";
import styles from "./Footer.module.scss";

const Footer = () => {
  return (
    <footer>
      <Contacts />
    </footer>
  );
};

export default Footer;
